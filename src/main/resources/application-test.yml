
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password:
  # H2 데이터베이스 방언 설정
  jpa:
    hibernate:
      dialect: org.hibernate.dialect.H2Dialect